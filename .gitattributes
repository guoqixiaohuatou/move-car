# 统一换行符，避免 Windows / macOS 协作者互相刷全文件 diff
* text=auto eol=lf

# shell 脚本必须是 LF，否则在 Linux/macOS 上执行会报 bad interpreter
*.sh text eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
