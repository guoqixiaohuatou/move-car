# 挪车小程序 · 体验测试指南（trial 模式）

> 适用阶段：正式发布前，用「体验成员 + 真机」跑通完整流程。
> 当前 `RUN_MODE = trial`，生成的码只指向体验版，**只有体验成员扫得开**。

---

## 0. 两个角色要分清

| 角色 | 谁 | 干什么 |
|---|---|---|
| **车主（你）** | 小程序主人 | 登记车辆、开微信通知、生成挪车码、收通知 |
| **扫码方（体验成员）** | 帮你测的人 / 你自己另一台手机 | 扫挪车码、看到手机号、发通知 / 拨号 |

> 单人自测也行：自己扫码通知自己（通知会发回自己），但**跨账号通知**建议找一位体验成员用他的手机扫，更真实。

---

## 1. 前置：把测试人加为体验成员

公众平台（mp.weixin.qq.com）→ 左侧「管理」→「用户身份」→「体验成员」→ 点「+」→ 输入对方微信号 → 对方在微信里确认。

- 你自己（管理员/开发者）默认就是体验成员，可用自己手机扫。
- 想测「陌生人扫码」效果，就加一位朋友为体验成员，用他的微信扫。

---

## 2. 用真机打开体验版

开发者工具 → 顶部「预览」→ 用你的微信扫预览码 → 手机上打开的是**体验版**小程序。

> ⚠️ 订阅消息授权（`wx.requestSubscribeMessage`）**只在真机有效**，开发者工具模拟器里点「微信通知」只会报错/不弹窗。所以整轮测试必须在手机上做。

---

## 3. 车主端测试（你操作）

1. 首页 → 「添加车辆」（edit 页）
2. 填 **车牌号**（必填，自动转大写校验，如 `京A12345`）、**手机号**（选填，建议填以便测拨号）、车型（选填）
3. 点「保存」→ 弹「不填手机号？」确认框（只有留空才出现）；填了就直接过
4. 首次建码会弹 **「请挪车提醒」订阅授权** → 勾选「允许」→ 提示「开启成功，当前可通知 N 次」
5. 自动跳到「挪车码」页（code 页）
6. 点「保存小程序码到相册」→ 授权相册 → 保存到手机

✅ 车主侧验证点：车辆保存成功、订阅授权弹窗且额度累积、小程序码生成并存入相册。

---

## 4. 扫码方测试（体验成员 / 你自己扫）

在**体验成员**的手机上：

1. 微信 →「扫一扫」→「从相册选择」刚保存的挪车码图片（或长按图片识别二维码）
2. 进入 notify 页，应看到：
   - 车牌（脱敏显示，如 `京A***5`）
   - 车型（若填了）
   - **手机号明文**（因 `SHOW_PLAIN_PHONE=true`）+「拨打电话」「复制号码」
   - 快捷留言（4 条）+ 自定义输入框
   - 「微信通知」按钮
3. 点「微信通知」→ 车主手机应收到「挪车通知」订阅消息
4. 点「拨打电话」→ 真机调起拨号界面

✅ 扫码侧验证点：码能打开（体验成员才行）、手机号明文可见、通知送达车主、拨号能调起。

---

## 5. 跨账号真实测试（推荐补做）

1. 让一位体验成员用**他的手机**扫你的码
2. 他点「微信通知」→ **你的手机**收到订阅消息
3. 他点「拨打电话」→ 调起拨你的号

这一步验证：通知确实发给了**车主 openid**，而非扫码人自己。

---

## 6. 逐项勾选表

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 添加车辆成功 | ☐ |
| 2 | 订阅授权在真机弹窗且额度累积 | ☐ |
| 3 | 生成小程序码并保存到相册 | ☐ |
| 4 | 体验成员扫码能打开 notify 页 | ☐ |
| 5 | 手机号明文显示 | ☐ |
| 6 | 微信通知送达车主 | ☐ |
| 7 | 拨打电话调起 | ☐ |
| 8 | 快捷留言发送（可选） | ☐ |
| 9 | 跨账号通知（体验成员扫→你收到） | ☐ |

---

## 7. 常见测试坑

| 现象 | 原因 | 解决 |
|---|---|---|
| 点「微信通知」无弹窗/报错 | 在开发者工具模拟器里测 | 必须真机 |
| 扫码提示「无权限访问」 | 扫码人不是体验成员；或码是体验版用非成员微信扫 | 加为体验成员再用其微信扫 |
| 通知收不到 | 车主没在真机授权订阅 / 额度用完 / 没选模板 | 真机重新授权；云端测试跑 `{}` 看模板项 |
| 相册保存失败 | 相册权限未开 | 弹窗点「去设置」开 `scope.writePhotosAlbum` |
| 相册保存失败 | 相册权限未开 | 弹窗点「去设置」开 `scope.writePhotosAlbum` |
| 自己扫自己码也行 | 通知发给自己，属正常自测 | 想测跨账号见第 5 步 |

---

## 8. 测完之后

体验测试通过 ≠ 路人能用。要让真路人扫码，仍需：
1. ICP 备案（最慢，现在就提）
2. 公众平台配置隐私保护指引
3. 备案后发布正式版 → 首页开关切 `release` → 删旧车重新生成码

发布当天，用正式版码再走一遍第 4–5 步即可。
