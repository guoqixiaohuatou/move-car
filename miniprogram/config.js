/**
 * 全局配置
 * ------------------------------------------------------------
 * 使用前需要填写一项：
 *   CLOUD_ENV —— 云开发环境 ID（微信开发者工具 > 云开发 > 环境 ID）
 *                留空则使用默认环境（账号下只有一个环境时可以留空）
 *
 * 订阅消息模板（微信通知）已做「自动发现」，一般无需手动填 TEMPLATE_ID：
 *   云端会自动读取你账号下选用过的模板，优先挑标题含「挪车 / 移车」的那一个，
 *   并按字段中文名自动映射（车牌 / 内容 / 时间 / 车型）。
 *   详见 README 第 4 节。
 * ------------------------------------------------------------
 */

module.exports = {
  // 云开发环境 ID，形如 'move-car-1x2y3z4abcd'
  CLOUD_ENV: 'YOUR_CLOUD_ENV_ID',

  // 云函数名称（与 cloudfunctions/ 下的目录名一致）
  CLOUD_FUNC: 'car',

  // 订阅消息模板 ID（可留空）
  // 留空 = 自动发现；填了则以这里为准。
  // 在公众平台 > 功能 > 订阅消息 > 我的模板 里可查看已选用的模板 ID。
  TEMPLATE_ID: '',

  // ------------------------------------------------------------
  // UI / 交互开关
  // ------------------------------------------------------------

  /**
   * 扫码页是否明文展示车主手机号
   *   true （默认）—— 页面直接显示完整号码，扫码方点一下就能拨
   *   false        —— 只显示 138****8888，点击「拨打」时由微信系统弹窗显示
   *
   * 说明：市面挪车码基本都是明文展示（本来就是为了让别人联系你）。
   * 但明文意味着号码会被路人看到、可能被拍摄采集，介意的话改 false。
   */
  // ⚠️ 已为「公开发布上线」改为 false：明文手机号会被任意路人看到/拍摄采集，
  //    且审核可能以「用户信息安全」为由要求整改。若仅自己/体验成员调试可改回 true。
  SHOW_PLAIN_PHONE: false,

  // 拨号冷却：同一个扫码人 N 秒内不可重复通知同一辆车
  NOTIFY_COOLDOWN_SEC: 60
}
