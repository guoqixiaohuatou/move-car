# 免审核测试方案

> 目标：**把「提审 → 等 1–3 天 → 发布 → 发现不对 → 重来」的循环，压缩成几分钟一次的自检。**

---

## 一、先认清一件事：三类改动，审核成本完全不同

| 改动位置 | 要审核吗 | 生效方式 | 生效速度 |
|---|---|---|---|
| **云函数**（`cloudfunctions/car`） | ❌ 不需要 | 右键 → 上传并部署 | **立刻** |
| **数据库配置**（`sys_config/global`） | ❌ 不需要 | 控制台直接改 | **立刻** |
| **前端页面**（`miniprogram/`） | ✅ 需要 | 上传 → 提审 → 发布 | 1–3 天 |

**这意味着：**

- 运行模式、管理员、出码版本、模板、通知逻辑、绑定逻辑 → **全在云函数里，随时改随时测，不用审核**
- 只有**页面长什么样、按钮在哪**这类改动才需要提审

> ⚠️ 你已经踩过的坑就是典型：Bug 1/2/4 全都修在云函数里，却因为要「发布」才能验证而反复等审核。
> 实际上云函数部署完，下面这套方法**几分钟就能验完**。

---

## 二、四层测试：从最便宜到最贵

### L1　云函数 / 数据库自检（0 审核，30 秒）

云开发控制台 → 云函数 `car` → **云端测试**，直接贴这些：

| 想验什么 | 粘贴的内容 | 看什么 |
|---|---|---|
| 整体健康 | `{"action":"health","payload":{}}` | 集合 / 模板 / 管理员是否都 OK |
| 当前出码版本 | `{"action":"getRunMode","payload":{}}` | `envVersion` 是 `release` 还是 `trial` |
| 切成正式版 | `{"action":"setRunMode","payload":{"runMode":"release"}}` | `persisted: true` |
| **某张码扫了会怎样** | `{"action":"dryrun","payload":{"codeId":"你的码ID"}}` | **`verdict` 字段，一句人话结论** |
| 模板是否就绪 | `{"action":"templateInfo","payload":{}}` | `ready: true` |

**`dryrun` 是新加的，最省时间**——不用扫码就能预判：

```json
{
  "runMode": "release",
  "envVersion": "release",
  "code": { "codeId": "xxx", "bound": true, "plate": "京A12345",
            "enabled": true, "quota": 3, "wxacodeEnv": "release" },
  "envMatch": true,
  "verdict": "正常：绑定了 京A12345，剩余额度 3 次，扫码可通知车主。"
}
```

它会直接告诉你这些坑：码图版本与运行模式不一致、未绑定、已停用、额度为 0、模板未就绪、压根没出过图。

---

### L2　开发者工具模拟扫码（0 审核，1 分钟）

**不用真的扫码**。项目里已经配好一个编译条件：

> 开发者工具 → 编译模式下拉 → 「**扫码通知页（真机扫码入口）**」

它直接用 `pages/notify/notify?scene=xxx` 启动，等价于扫了一张码。

**要改的地方**：把 `scene` 换成你真实的码 ID（首页「我的挪车码」里长按可复制）。
路径：`project.config.json` → `condition.miniprogram.list[0].query`。

这一层能验：页面渲染、绑定流程、拨号、通知按钮、额度扣减——**除了「扫不扫得开」以外的全部逻辑**。

---

### L3　体验版真机扫码（0 审核，但要设一次体验版）

这是验证**真实扫码链路**的唯一免审方式。

**只需设一次：**

1. 开发者工具 → 右上「**上传**」（传到开发版本）
2. 公众平台 → 管理 → **版本管理** → 开发版本卡片 → 「**选为体验版**」
3. 公众平台 → 管理 → **成员管理** → 体验成员 → 添加测试微信号

**然后：**

- 运行模式保持 `trial` → 生成的就是体验版码
- 体验成员（包括你自己和加进来的号）扫得开
- 完整走通：扫码 → 打开 → 绑定 / 通知

> ⚠️ **你之前报的「小程序体验版，无法体验」，根因就是第 2 步没做**——从来没设过体验版，体验版码当然扫不开。
> 这个和「是不是正式版」是两件事，别混在一起。

---

### L4　路人能否扫开（必须有线上版本）

这是唯一绕不过审核的一层。但有个技巧能省掉大部分等待：

> 💡 **release 码你自己也能扫。** 正式版是公开的，不限体验成员。

**做法：**

1. 确认出码版本是正式版（云端测试 `getRunMode` → `envVersion` 应为 `release`），
   然后在小程序里「＋空白挪车码」生成一张，或直接打开已有车辆的挪车码页让它重生成。
   > `createBlanks` 需要管理员身份，只能从小程序里调；控制台调不了，别在这里浪费时间。

2. **立刻自己扫这张码**：
   - 正常打开 → 说明**已经有线上版本**，码本身没问题 ✅
   - 提示「该小程序已发布版本不存在」→ 还没发布过，**这时才需要等审核**

3. 等审核通过并发布后，**同一张码再扫一次**——这一次验证的才是新前端。

**关键点**：第一版发布之后，「线上版本」就一直在了。之后你生成的 release 码**随时都能扫开验证**，只是打开的页面是已发布的那个前端。所以：

- 验「**码对不对**」→ 随时可扫，0 等待
- 验「**新前端对不对**」→ 只能走 L2/L3，或者提审

---

## 三、推荐工作流（照这个来，基本不用等审核）

```
① 改云函数/配置  →  部署  →  云端测试 dryrun / getRunMode   （30 秒）
② 改前端逻辑     →  开发者工具自定义编译条件 / 真机调试跑通  （0 审核）
③ 要验真实扫码   →  设为体验版 + 加体验成员，扫 trial 码     （0 审核，设一次长期有效）
④ 前端改动攒够一批 → 一次性提审 → 发布
⑤ 发布后        →  扫 release 码做最终确认
```

**最容易犯的错：改一点前端就提一次审。** 攒着一起提。

---

## 四、让以后尽量不用提审

前端一旦发布就锁死在用户手里，之后每次改动都要审核。所以：

| 能放云端的，别写死在前端 | 现在的状态 |
|---|---|
| 运行模式（trial / release） | ✅ 已是云端配置 `sys_config/global.runMode` |
| 管理员 openid | ✅ 已是云端配置 `sys_config/global.adminOpenid` |
| 订阅消息模板 | ✅ 云端自动发现 |
| 出码版本 | ✅ 云端决定，前端只传意图 |
| 内容安全校验 | ✅ 在云函数 |
| 页面文案 / 布局 / 按钮 | ❌ 前端，改了要审核 |

**结论**：以后做新功能，先问一句「这个逻辑能不能放云函数」。能放就放，改起来不用等。

---

## 五、提审前 30 秒检查清单

- [ ] 云端测试 `health` → 全绿
- [ ] 云端测试 `getRunMode` → `envVersion` 符合预期
- [ ] 云端测试 `dryrun`（带最近的码 ID）→ `verdict` 没有 ⚠️
- [ ] 开发者工具里用编译条件把扫码页跑一遍
- [ ] 真机调试跑一遍（开发者工具「真机调试」按钮）
- [ ] 确认已「选为体验版」、测试号已加为体验成员
